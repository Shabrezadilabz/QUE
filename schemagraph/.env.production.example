# Que UI — production / Vercel
# Set in Vercel → Project → Settings → Environment Variables (Production).

# Render (or other) public API origin — no trailing slash
VITE_STITCH_API_URL=https://que-api.onrender.com
