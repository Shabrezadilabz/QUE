/**
 * Placeholder re-export — real implementation lives in components/MainCanvas.
 */
export { MainCanvas } from '@/components/MainCanvas'
export type { MainCanvasProps } from '@/components/MainCanvas'
